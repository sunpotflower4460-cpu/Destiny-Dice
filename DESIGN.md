# Intention Dice（仮）— 引き寄せ実証ラボ 設計図 v2.0

一行コンセプト: 信じる人も疑う人も、同じデータの前で頷ける「1年間の意図実験装置」。

v2.0の変更点: Layer C（願いのランダム化比較試験）を追加し、三層実験に。同時に「シンプルさの原則」を設計の柱に昇格⸺ユーザーが意識するのは3つだけ、残りは全部裏で自動。

## 0. 設計の核となる5つの判断

1. **サイコロの皮を被ったビットストリーム測定器** 1日1回サイコロを振るだけでは年365試行しか得られない（1/6の的なら期待61回、2σで±14回の揺らぎに効果が埋没）。そこで1回の「抽選」の内部を量子乱数1,024ビットにする。体験は「サイコロを1回振る」、データは「コイン投げ1,024回」。年間で条件あたり約7.5万試行になり、±0.5ポイントの偏りまで検出できる。

2. **逃げ道を仕組みで塞ぐ** 体験談が実証にならない理由は、効果の有無以前に「記録の削除」「後出しの解釈」「都合のいい打ち切り」ができる構造にある。本アプリは、事前登録ロック＋追記専用ハッシュチェーン台帳＋削除UIなし＋全数値に偶然期待値を併記、でその構造自体を潰す。

3. **三層実験（物理・心・人生）**
   - Layer A（物理）: 意図は量子乱数を曲げるか。量子ビットは信念に反応しない物理現象なので、結果がどう出ようと「思い込み」では原理的に説明できない。
   - Layer B（心）: 実践は自分の気分・エネルギーを変えるか。プラセボ込みでいい（日常で得られる効果そのものだから）。
   - Layer C（人生）: 実践は願いの実現率を変えるか。個人実験の最大の弱点「やらなかったら起きなかったのか？（反実仮想）」に答えられる道具は事実上ひとつ⸺ランダム化。願いを実践群と封印群にランダムに分け、封印群をベースラインにする。「人生イベントの確率は計算できない」問題が消える。
   - どの層で結果が出ても発見。三層あることで、効果が出た場合に「物理チャネルか、行動・縁チャネルか」の三角測量ができる。

4. **確証（Confirmatory）と探索（Exploratory）の分離** 「厳密に実証する」と「なぜ効いたか・特徴を探る」は放置すると衝突する（自由に探すと偶然のパターンが必ず見つかり、実証が汚れる）。だから完全に分ける。
   - 確証レイヤー: 事前登録した主要検定を、実験終了時に1回だけ実行。判定ルールも事前固定。これが「できたことは実証になる」を担保する。ここでは一切の後出しをしない。
   - 探索レイヤー: 「なぜ」「特徴は」「予言は当たったか」を掘る全分析。あくまで次の実験の仮説の種であり、それ自体は実証としない（多重比較の注意書きを自動表示）。

5. **シンプルさの原則（v2.0で柱に昇格）** ユーザーが意識するのは3つだけ： ① 毎日開いてセッションする（2〜10分） ② 願いを思いついたら放り込む（15〜30秒） ③ 締切通知が来たら○×をつける（10秒） 層別・割付・封印・統計・台帳・判定ルール・公証は全部裏で自動。重い決めごとは初日の登録ウィザード（約10分）に集約し、毎日は軽く。UI原則：ホームは禅、ラボ（統計）は別タブ。数字を見たい時だけ見に行く。

## 1. 目的

- 5つの実践パターン（引くだけ／意図書き／アファメーション／祈り／全部）が量子乱数の出目に与える影響を、1年間・事前登録・改竄検知可能ログのもとで測定する（Layer A）
- 各実践の心理的効果（気分・エネルギーの pre/post 変化）を測定する（Layer B）
- 実践が願いの実現率を変えるかを、願いのランダム化比較で測定する（Layer C）
- 実験終了時、出荷時から同梱の分析コードが自動でレポートを生成する（後出し分析の排除）

### 1.1 科学的前提（正直に）

- ミクロPK（意図による乱数への影響）は、過去最大級の研究（プリンストンPEAR研、12年・数百万試行）でも主張された効果は0.02%程度で、独立追試では再現されなかった、というのが現状。
- Layer A の検出力は条件あたり±0.5ポイント。問うのは「PEAR級の極微効果」ではなく「日常で体感できる強さの引き寄せが物理レベルで存在するか」。
- Layer C は経路を限定しない。実践→モチベーション→行動→実現、という「普通の心理経路」も含めて丸ごと測る。乱数が曲がらなくても人生スケールの引き寄せは別問題⸺その両方に別々の答えを出すのが三層構造の意味。

### 1.2 「たまたま・思い込みには絶対ならない」への正直な回答

思い込み（プラセボ・記憶バイアス）に対しては「絶対」を作れる。Layer A は物理乱数なので信念では動かせない。Layer C は封印群という反実仮想があるので「やったから起きた気がする」が通用しない。台帳が改竄不能なので記憶の書き換えも効かない。

たまたま（偶然）に対しては、統計上「絶対」は誰にも作れない。だが本設計は「偶然だと言うなら、この確率と戦ってください」という状態を作る：事前登録＋高検出力＋多重補正＋ベイズファクター。判定ラインを越えたら、事前に固定された小さな数値を正面から否定しない限り「たまたま」とは呼べない。

アプリの立場は「偶然を不可能にする」ではなく「偶然で片付けようとする人に、具体的な確率を突きつける」。

### 1.3 証拠の等級（レポートに自動表示）

| 層 | 等級 | 理由 |
|---|---|---|
| Layer A | ★★★ | 物理計測・自己申告なし・思い込み原理的に不可能 |
| Layer C | ★★ | ランダム化あり・ただし実現判定が非盲検（自己判定） |
| Layer B | ★ | 非盲検・プラセボ込み（それでいい層として設計） |

等級を隠さず併記することが「他人から見ても信頼できる」の第一歩。

## 2. スコープ

**IN（v1）**: シングルユーザー／ローカルファースト／Layer A（5条件＋機械コントロール・量子乱数・フォールバック付き）／Layer B（気分pre/post）／Layer C（願いレジストリ・量子割付・封印・締切判定）／ハッシュチェーン台帳／事前登録／統計ダッシュボード／最終レポート／ローカル通知／エクスポート／週次チェーンヘッド公開（任意ON・自動）

**OUT（v1ではやらない）**: マルチユーザー集計・クラウド同期（エクスポートのみ）・SNS共有・課金・Android・AI判定や第三者ブラインド判定・他者の願いデザイン・写真証拠添付・OpenTimestamps本格版（→v2）

## 3. 実験デザイン

### 3.0 ユーザー体験の全体像（シンプルさ設計）

| いつ | やること | 所要 |
|---|---|---|
| 最初の1回 | 登録ウィザード（実験パラメータ・予言の立て札・判定ルール確認→ロック） | 約10分 |
| 毎日 | セッション（条件別の実践→抽選→最後に願いタイム30〜60秒） | 2〜10分 |
| 思いついた時 | 願いクイック登録（テキスト1行＋チップ3つ） | 15〜30秒 |
| 締切通知が来た時 | ○×判定（＋実現時のみ経路チップ1タップ） | 10秒/件 |

それ以外⸺割付・封印管理・層の記録・統計計算・台帳連鎖・公証送信⸺は全部自動。ユーザーが統計を意識するのは「ラボ」タブを自分で開いた時だけ。

### 3.1 1日の流れ（2〜10分）

1. アプリを開く → その瞬間、コントロール抽選（機械のみ・無意図）を裏で実行し台帳に記録
2. 今日の条件を開示（年間スケジュールは登録時に生成・ハッシュ固定済みだが、表示は当日分のみ）
3. 気分チェック pre（気分・エネルギー 各1〜10、スライダー2つ・約5秒）
4. 今日の的を開示: HIGH または LOW（セッションごとに50/50でランダム決定し、抽選前に台帳へ記録）
5. 条件ごとの実践（3.2参照）
6. 予言（抽選前に台帳へ先行コミット）: §3.2.5参照。手応え0〜100＋任意の一言予言を記録 → この prediction エントリを先にハッシュ確定
7. 抽選: ボタン長押し3秒（念じながら）→ 量子乱数1,024ビット取得 → 演出つき開示。台帳の順序（prediction → session）が「予言が結果より前だった」ことを暗号的に証明する
8. 気分チェック post
9. 願いタイム（30〜60秒・自動挿入）: 実践群の願いカードが表示され、呼吸しながら意図を向ける。封印群は決して表示されない。P1〜P5全条件の日に毎日実施（Layer Aの抽選が完了した後なので A を汚さない。C の実践量が条件によらず一定になる）
10. 累積グラフをひと目見て終了。|z|≥2で「共鳴」、的方向 z≥+3 で「ミラクル」演出。予言の当否も即フィードバック

### 3.2 Layer A の条件（5＋1）

| # | 名前 | 実践内容 | 所要 | 有効判定 |
|---|---|---|---|---|
| P1 | 引くだけ | 実践なし。60秒の静かな待機（乱数レート制限も兼ねる） | 約2分 | 待機完了 |
| P2 | 意図書き | 「今日の的が実現した状態」を文章で書く | 約3分 | 30文字以上 |
| P3 | アファメーション | 登録時に自分で決めた宣言文を表示し、5分タイマー | 約6分 | タイマー完走 |
| P4 | 祈り | 3分タイマー＋任意で432Hz音源（自作曲をインポート可） | 約4分 | タイマー完走 |
| P5 | フルコンボ | P2→P3→P4 を連続実施 | 約10分 | 全完了 |
| C0 | コントロール | アプリ起動時の機械抽選。人間の意図なし | 0分 | 自動 |

- 全実践の対象は常に「今日の的（HIGH/LOW）」に統一する（条件間比較の前提）
- 実施ログ（秒数・文字数・本文）を台帳に記録 → コンプライアンス分析が可能

### 3.2.5 予言（Prediction）— 2階層

**(a) 手法ごとの立て札（登録時・genesisに固定）** 条件ごとに1つ、自分の仮説＝予言を自由記述で書く。例:「P4祈りは的方向に強く出るはず」「P1引くだけは0.5から動かないはず」。1年後にこの予言の答え合わせをする。結論ではなく仮説を先に宣言し、後から書き換えない⸺フラットさの担保。

**(b) セッションごとの手応え（抽選前に台帳へ先行コミット）** 毎回の抽選直前に記録：
- 手応えスライダー 0〜100: 「今日、的に届く確信」
- 一言予言（任意・短文）
- 順序が命: prediction エントリを先に確定 → その後に量子ビット取得。台帳の連鎖が「予言→結果」の時系列を暗号的に証明する（後出し予言は構造上不可能）

価値: 独立した測定になる。「あなたの直感は、実際に成功するセッションを予測できるか？」⸺平均効果がゼロでも、手応えが高い日だけ的中率が上がるならそれ自体が発見。無相関なら「主観的確信は物理に届いていない」という明快な結論。答え合わせは探索レイヤー（§3.5.5）。

### 3.3 乱数仕様

- 1セッション = 1,024ビット（登録時に 1,024 / 2,048 / 4,096 から選択、以後ロック）
- 1日のセッション数: 1（登録時に1〜3を選択可。条件は日内で固定、以後ロック）
- 取得順: ANU量子乱数（Cloudflare Workerプロキシ経由）→ random.org → ローカル crypto.getRandomValues。使用ソースを必ず記録
- 取得間隔ガード: 70秒以上（ANU無料APIのレート制限対応。最新の制限・仕様は実装時に要確認）
- ビット→hit変換: `hit = (bit == 今日の的)`。的が独立な50/50ランダムなので、乱数源そのものに偏りがあっても hit率の帰無仮説は正確に0.5（重要な設計上の保険）
- 願いの割付（Layer C）も同じフォールバック連鎖から1ビット取得し、ソースを記録（「願いの運命も量子が決める」）

### 3.4 スケジュール割付（Layer A）

- 5日ブロックのランダム順列を繰り返す（seed付きシャッフル）。365日分を登録時に一括生成
- スケジュール全体は登録ハッシュに含めて固定、表示は当日分のみ（「気分が乗った日に祈りを選ぶ」選択バイアスの防止）
- 欠測日は欠測のまま記録。日付基準で進むため条件バランスはほぼ保たれる
- 日付境界は 03:00（登録時に変更可、以後ロック）

### 3.5 測定と統計

**Layer A 主要評価（事前登録・365日目に1回だけ自動実行）**

各条件の hit率 vs 0.5、両側二項検定（正規近似）、Holm法で5検定を補正、α=0.05

事前登録の判定ルール（＝「実証」の定義。登録時にコードとして固定し、後から動かさない）

| 結果 | 頻度論 | ベイズ | 結論ラベル |
|---|---|---|---|
| 陽性（効いた） | Holm補正後 p < 0.01 | かつ BF10 > 30 | 実証: この条件は乱数を動かした |
| 陰性（効かない証拠） | — | BF10 < 1/30 | 実証: この条件は効かなかった |
| 判定保留 | 上記どちらも満たさない | | データ不足・継続 or 次実験へ |

- 「効いた」も「効かなかった」も積極的な結論として扱う（フラットさの本体）。「よくわからない」も正直に「保留」と出す。
- この表を越えた陽性は、事前に宣言した数値を否定しない限り「たまたま」と呼べない＝できたことがちゃんと実証になる。

**Layer A 副次**

- 条件間の異質性（χ²）／ P2–P5プール vs P1 ／ C0 vs 0.5（乱数品質QC）／ 時系列トレンド
- ライブ表示: 累積偏差グラフ `D_k = Σ(hit − 0.5)`、95%包絡線 `±0.98√k`、および逐次監視に耐えるベイズファクター
  - `lnBF10 = lgamma(1+y) + lgamma(1+n−y) − lgamma(2+n) + n·ln2`（Beta(1,1)事前、y=hit数）
  - ゴールデン値: n=8, y=8 → BF10 = 28.444… ／ n=8, y=4 → BF10 = 0.4063…
- 注: 年間の途中経過では頻度論のp値を出さない（覗き見問題）。途中はグラフとBFのみ、確定判定は最終1回

**Layer B（心）**

- 各条件の post−pre 平均変化＋95%CI（対応あり）
- 盲検不能＝プラセボ込みの効果測定。それでいい（日常で得られる効果そのものだから）とアプリ内に明記

**Layer C 主要評価（事前登録・実験終了時に1回だけ自動実行）**

- 対象: 締切が実験終了までに到来した全願い。取り下げ＝未実現として集計（都合の悪い願いを消すゲーミングの防止）。締切未到来の願いは件数のみ報告し次実験へ持ち越し
- 検定: 実践群 vs 封印群の実現率。Fisher正確検定（両側）＋2×2ベイズファクター
  - `BF10 = B(1+y1, 1+n1−y1) · B(1+y2, 1+n2−y2) / B(1+y1+y2, 1+n1+n2−y1−y2)`（独立Beta(1,1)事前 vs 共通p）
  - ゴールデン値: n1=n2=2, y1=2, y2=0 → BF10 = 10/3 = 3.333…
- 判定ルール表は Layer A と同形式（p<0.01 かつ BF>30 → 実証: 実践は実現率を上げた／BF<1/30 → 実証: 変えなかった／他は保留）。「判定は非盲検（★★等級）」の注記を自動付記
- 副次（頑健性）: 起きやすさ・影響可能性を共変量にしたロジスティック回帰／層別実現率

**Layer A 検出力（80%、両側α=.05、条件あたり）**

| 設定 | 年間ビット数/条件 | 検出可能な偏り |
|---|---|---|
| 1,024 × 1回/日 | 74,752 | ±0.51pp |
| 1,024 × 3回/日 | 224,256 | ±0.30pp |
| 4,096 × 3回/日 | 897,024 | ±0.15pp |

PEAR級（±0.02pp）は本設計では検出不能（必要試行 約4,900万/条件）。この限界もアプリ内に明記する。

**Layer C 検出力（判定済み願い・各群n、ベース実現率30%想定、80%、α=.05）**

| 各群n | 検出できる上乗せ |
|---|---|
| 50 | 約 +27pp（30%→57%） |
| 75 | 約 +22pp |
| 100 | 約 +19pp |
| 150 | 約 +16pp |

→ 主役はマイクロ願い（締切2週間〜1ヶ月の小さな願い）。週2〜3個の登録で年120〜150個＝各群60〜75。検出できるのは「実現率がおよそ2倍になる」級の効果。それ未満（例: 30%→36%）は個人1年では原理的に検出不能⸺これも正直にアプリ内へ明記。大きな願いも登録してよい（探索レイヤーの材料になる）が、統計の柱にはならない。

### 3.5.5 探索レイヤー — 「なぜ効いたか・特徴は何か」（実証ではなく、次の実験の仮説の種）

確証レイヤーが「効いたか？」に答えるのに対し、こちらは効いた場合に「なぜ・どんな特徴か」を掘るための分析群。全レポートで「これは探索的分析。多重比較を含むため、ここで見えたパターンは実証ではなく仮説。確かめるには新しい事前登録実験が必要」と自動明記する。この壁があるからこそ、自由に掘っても確証レイヤーが汚れない。

**Layer A/B の探索**

1. 強度勾配（dose-response）— 最重要: P1(なし) < P2/P3/P4 < P5(フルコンボ) が単調に効果を増すか。同一条件内で、実践が長いほど・手応えが高いほど z が大きいか。「濃くやるほど効く」という単調関係は偶然では出にくく、単発の当たりよりずっと強い証拠になる
2. 習慣化・時間発展: 各条件で hit率をセッション番号に回帰。1年を4四半期に割って比較。練習を積むほど効くようになるか（学習曲線）／慣れて薄れるか
3. 状態依存: 気分pre・エネルギーpre が高い日ほど当たるか。時刻・曜日・月相（日付から自動計算・負担ゼロ）で変わるか
4. 予言の答え合わせ（キャリブレーション）: 手応え0〜100を層別し実際の的中率をプロット（校正曲線）。手応えと z の相関。登録時の「手法ごとの立て札」に ○/×/保留 を自動判定
5. ミラクル日のプロファイル: |z|が大きく出た日に共通する条件・気分・時刻・手応えを一覧化

**Layer C の探索**

6. 経路分解: 実現した願いの経路（自分の行動経由／他人がしてくれた／偶然の出会い／不明）の分布を群間比較。効果が「自分の行動経由」のみなら動機づけ・行動変化の経路。「自分で動かせない」願いでも群差が出るなら、それは説明の難しい発見
7. 層別の効きめ: 起きやすさ×効果、影響可能性×効果の交互作用
8. 実現までの日数: 実践群の方が早く実現するか
9. 願い文の特徴: 実現した願いに共通する書き方・具体性・長さ

探索→確証のループ: ここで見つかった「特徴」（例: 手応えが高い朝の祈りだけ効く、自分で動かせる小さな願いだけ効く）は、翌年の新実験で事前登録された予言として検証できる。1年目=まず効くか＋特徴探し、2年目=その特徴を狙い撃ちで実証。

### 3.6 ミラクルの確率設計

- 共鳴: セッション |z|≥2 → 偶然でも年約17回
- ミラクル: 的方向 z≥+3 → 偶然でも年約0.5回（≒2年に1回）
- 正直メーターの原則: アプリ内の全カウント・全数値の隣に「偶然ならこのくらい: ◯回」を常時併記する。Layer C では封印群がそのまま正直メーター（「封印群があなたの世界のベースライン」と表示）

### 3.7 Layer C — 願いレジストリの仕組み（全部裏で自動）

**登録（15〜30秒）**: ホームの「＋」から。

- テキスト1行（成功が一目で分かる文に。プレースホルダが例をローテーション表示:「1月中に◯◯さんから連絡が来る」「今月、探していた◯◯が手に入る」）
- チップ3つ: 締切（2週間／1ヶ月／3ヶ月、デフォルト1ヶ月）・起きやすさ（よく起きそう／五分五分／めったに）・自分で動かせる？（動かせる／半々／動かせない）
- 判定不能になりそうな曖昧な文にはヒント1行を表示（ブロックはしない）

**割付（自動・即時）**: 登録した瞬間、量子1ビットで実践／封印を決定し台帳へ（wish → assignment の順で連鎖。人の選択は一切挟まない）。表示は一言だけ:

- 「この願いは実践に選ばれました。毎日の願いタイムに現れます」
- 「この願いは封印されました。締切まで、そっと預かります」

**封印**: 封印群の願いは締切まで非表示（触らない・見ないを仕組みで支援）。※自己盲検は不可能。非表示は接触の低減であって盲検ではない、と設定画面に正直に注記

**願いタイム**: 毎日セッション末尾に30〜60秒、実践群カードを自動表示（§3.1 手順9）。ユーザーが「今日はどの願いをやるか」を考える必要はない

**判定（通知駆動・10秒/件）**: 締切日に通知 → 事前登録した成功基準を表示 → 実現した／しなかった／判定不能。実現時のみ経路チップ1タップ（自分の行動／他人がしてくれた／偶然の出会い／不明）＋任意メモ

**取り下げ**: 削除は不可。「取り下げ」記録のみ可能で、未実現として集計（件数はレポートに明記）

**外部公証（自動・設定でON/OFF）**: 週1回、チェーンヘッドのハッシュを qrng-proxy Worker の公開ログへ送信（GETで誰でも閲覧可）。オフライン時はスキップして次週。→「この願いと成功基準は結果より前に存在した」を第三者が自力検証できる。本格的なOpenTimestamps対応はv2

### 3.8 台帳（改竄検知）

- 単一テーブル ledger に registration / control / prediction / session / wishmoment / wish / assignment / judgment を時系列で追記
- `entry_hash = SHA-256(prev_hash + 正規化JSON)`、genesis = 登録パラメータのハッシュ
- `verifyChain()` をいつでも実行可能。エクスポート（JSON/CSV）には全生ビットを含める
- 削除・編集の経路はコード上存在させない

### 3.9 既知の限界（最終レポートにも自動掲載）

n=1／Layer B・Cは盲検不能（Cの実現判定は自己判定＝★★等級）／条件の日替わりはキャリーオーバー無視の仮定／封印群への注意を完全遮断はできない（登録した記憶は残る）／類似願いの重複はノイズ源（ただし偽陽性方向には働かない）／マイクロ願いの代表性（小さな願い≠人生の大願）／外部API依存／Layer Aの検証対象は「物理を直接曲げる形の引き寄せ」のみ／ミラクル演出自体が期待や気分に影響する可能性

## 4. スタック

- Capacitor 6 + React 18 + TypeScript + Vite（標準構成）
- DB: @capacitor-community/sqlite
- ハッシュ: WebCrypto SubtleCrypto（SHA-256）
- 通知: @capacitor/local-notifications（毎日リマインダー＋願い締切通知）
- グラフ: Canvas自作（累積偏差）＋必要なら recharts
- 音源: ローカルファイルインポート（432Hz自作曲）
- `workers/qrng-proxy`: Cloudflare Worker（キー秘匿・リトライ・フォールバック＋週次チェーンヘッド公開ログのGETエンドポイント）。未デプロイでもローカル乱数で全機能動作すること（公証は自動スキップ）

## 5. データモデル

```sql
-- 台帳（追記専用・単一チェーン）
CREATE TABLE ledger (
  seq INTEGER PRIMARY KEY AUTOINCREMENT,
  type TEXT NOT NULL,       -- 'registration'|'control'|'prediction'|'session'|'wishmoment'|'wish'|'assignment'|'judgment'
  payload_json TEXT NOT NULL,
  created_at TEXT NOT NULL,
  prev_hash TEXT NOT NULL,
  entry_hash TEXT NOT NULL
);
```

```ts
type RegistrationPayload = {
  experimentId: string; startDate: string; days: number;              // 365
  bitsPerDraw: 1024 | 2048 | 4096; sessionsPerDay: 1 | 2 | 3;
  dayBoundaryHour: number;                                            // 3
  affirmationText: string;
  predictionByCondition: string[];                                    // 5要素: 手法ごとの立て札(予言)
  decisionRuleA: { pThresh: number; bfPos: number; bfNeg: number };   // 例 {0.01, 30, 1/30}
  layerC: {
    enabled: boolean;                                                 // 既定 true
    defaultDeadlineDays: number;                                      // 28
    withdrawalPolicy: 'count_as_fail';                                // 固定
    decisionRuleC: { pThresh: number; bfPos: number; bfNeg: number };
    notarize: boolean;                                                // 週次公証 ON/OFF
  };
  schedule: number[];                                                 // 365要素, 0..4
  scheduleSeed: string; analysisPlanVersion: string;
};

// 抽選"前"に確定する予言エントリ（sessionより必ず先に台帳へ）
type PredictionPayload = {
  date: string; seqInDay: number; condition: 0 | 1 | 2 | 3 | 4;
  targetDir: 0 | 1;
  confidence: number;              // 0..100 「的に届く手応え」
  prophecyText?: string;
  committedAt: string;
};

type ControlPayload = {
  date: string; rngSource: 'anu' | 'randomorg' | 'local';
  bitsHex: string; nBits: number; hits: number; z: number;            // 的なし→1の数を0.5と比較
};

type SessionPayload = {
  date: string; seqInDay: number; condition: 0 | 1 | 2 | 3 | 4;
  targetDir: 0 | 1; rngSource: string;
  predictionSeq: number;           // 対応する prediction の seq（順序=予言が先、を証明）
  bitsHex: string; nBits: number; hits: number; z: number;
  ritual: { kind: string; seconds: number; textLen?: number; text?: string; valid: boolean };
  moodPre: { v: number; e: number }; moodPost: { v: number; e: number };
  context: {
    hour: number; dow: number;     // 時刻・曜日（timestampから）
    lunarPhase: number;            // 0..1 月相（日付から自動計算）
    sleep?: number;                // 前夜の睡眠1..10（任意・日次）
    stateTag?: string;             // 'calm'|'scattered'|'tired'|'energized' 等ワンタップ（任意）
  };
  startedAt: string; completedAt: string;
};

// 毎日の願いタイム（自動記録）
type WishMomentPayload = {
  date: string; wishIdsShown: string[]; seconds: number;
};

// Layer C: 願い（登録時に確定・以後変更不可）
type WishPayload = {
  wishId: string;
  text: string;                                    // 成功が一目で分かる文
  deadline: string;                                // ISO date
  likelihood: 1 | 2 | 3;                           // よく起きそう/五分五分/めったに
  influence: 'self' | 'mixed' | 'external';        // 自分で動かせる？
  createdAt: string;
};

// 割付（wishエントリの直後に自動追記。順序が「基準が割付より前」を証明）
type AssignmentPayload = {
  wishId: string; arm: 'practice' | 'sealed';
  rngSource: string; bit: 0 | 1;
  committedAt: string;
};

// 判定（締切到来時）
type JudgmentPayload = {
  wishId: string;
  outcome: 'realized' | 'not_realized' | 'undecidable' | 'withdrawn';
  pathway?: 'own_action' | 'other_person' | 'chance_encounter' | 'unknown';   // realized時のみ
  note?: string;
  judgedAt: string;
};
```

UI用ビューは ledger から導出。キャッシュテーブルは可だが、正は常に ledger。

## 6. フェーズ分割（各フェーズ=1PR。完了基準は機械検証可能）

- **P0 足場**: Vite+React+TS+Capacitor(iOS)＋SQLite配線＋PROGRESS.md — `pnpm typecheck` `pnpm test` green ／ シミュレータで起動しプレースホルダ表示
- **P1 乱数モジュール** `src/rng/`: フォールバック連鎖・70秒ガード・ソース記録・テスト用シードモード・割付用1ビット取得API — fetchモックで3ソース分岐＋ガードの単体テスト green ／ ネット無しで動作
- **P2 台帳** `src/ledger/`: append / verifyChain / export(JSON, CSV)。全8タイプ対応 — 改竄フィクスチャで verify 失敗を検出するテスト green ／ エクスポート往復一致
- **P3 事前登録**: パラメータUI → 手法ごとの予言（立て札）入力 → 判定ルールA/C固定 → Layer C設定 → スケジュール生成（seed付きブロックシャッフル）→ ロック → genesis書込 — 各条件73±1回のバランステスト ／ 同一seed→同一スケジュールの決定性テスト ／ 当日以外非表示のロジックテスト ／ predictionByCondition・decisionRuleA・layerC が genesis ハッシュに含まれるテスト
- **P4 セッションフロー（Layer A/B）**: §3.1の手順1〜8＋10（実践タイマー・意図テキスト・予言の先行コミット・長押し抽選・気分スライダー）— モック乱数での1日E2E（擬似）が valid なチェーンentryを3件（control＋prediction＋session）生成 ／ prediction の seq が session の seq より小さいことを検証
- **P5 統計エンジンA** `src/stats/`: z / CI / 二項p / Holm / BF(一標本) / 累積偏差系列 / コントロールQC / 予言校正（confidence×z 相関・校正曲線）/ 強度勾配 / 四半期トレンド — ゴールデンテスト（BF既知値2件・Holm既知例・z既知例・校正曲線の既知例）全green ／ stats/ はUI非依存の純関数
- **P6 ダッシュボードA**: 条件別カード（偶然期待値を併記）・累積偏差Canvas・ミラクルログ・カレンダー。ホーム（禅）とラボ（数字）のタブ分離 — シミュレートデータ365日分でレンダリング崩れなし（スナップショット）
- **P7 願いレジストリ（Layer C）**: クイック登録UI・量子割付（wish→assignment連鎖）・封印管理（締切まで非表示）・締切キュー・判定フロー（○×＋経路チップ）・願いタイム（セッション末尾に自動挿入）・取り下げ — E2E: 登録→assignmentがwishの直後に連鎖 ／ 封印願いが一覧・願いタイムに出ないテスト ／ 締切到来で判定待ちに出る ／ judgment エントリ生成 ／ 取り下げ=未実現の集計テスト
- **P8 統計エンジンC＋ダッシュボード統合**: Fisher正確検定・2×2 BF・層別集計・実現率カード（「封印群があなたのベースライン」表示）・経路分布 — 2×2 BFゴールデン（n1=n2=2, y1=2, y2=0 → 10/3）／ Fisher既知例 ／ withdrawn→未実現の集計テスト green
- **P9 通知・継続UX**: 毎日リマインダー・願い締切通知・やさしいストリーク（罰なし）・03:00境界 — 境界時刻の日付判定ユニットテスト green ／ 締切通知のスケジューリングテスト
- **P10 最終レポート**: 2部構成で自動生成 → markdown → 共有シート。①確証パート（A判定表＋C判定表＋証拠等級注記のみ、後出しなし）②探索パート（§3.5.5の全分析＋多重比較の注意書き＋予言の答え合わせ）。両パートは視覚的に明確に分離 — `pnpm simulate --days 365 --effect 0 --wish-n 120 --wish-base 0.3 --wish-effect 0` のレポートがゴールデン一致 ／ `--effect 0.01 --condition 2` でA陽性 ／ `--wish-effect 0.25` でC陽性（固定seed）／ 確証パートに探索由来の数値が混入しないことのテスト
- **P11 仕上げ・申請**: アイコン／スプラッシュ／fastlane（100年時計パイプライン流用）／ストア文言／Worker公開ログエンドポイント — TestFlight配信完了 ／ 公開ログGETで週次ハッシュ閲覧可。※ストア表現は「超常現象の証明」ではなく「1年間のパーソナル実験・記録アプリ」のトーンで（審査安全）

## 7. 機械検証可能な完了基準（全体）

- 全フェーズで `pnpm typecheck && pnpm test` green を維持
- `pnpm simulate` による365日シミュレーション（願い120件含む）→ verifyChain 成功 → レポート生成、がCIで再現可能
- `src/stats/` の全関数にゴールデンテスト
- ledger に削除・更新を行うコードが存在しない（grepベースの簡易チェックをCIに含める）

## 8. 不変ルール

1. ledger は追記のみ。削除・編集の経路を実装しない（デバッグ用も不可）
2. 事前登録後のパラメータ変更禁止。変えたければ新実験IDで最初から
3. bits→hits→z の経路にUI都合の加工を入れない。統計は src/stats/ に隔離・純関数・テスト必須
4. すべての表示数値の隣に「偶然ならこのくらい」を併記する（正直メーター）
5. 欠測は欠測のまま。補完・後付け除外のロジック禁止
6. rngSource を必ず記録し、local フォールバックの発生を隠さない
7. 予言（prediction）は必ず量子ビット取得より前に台帳へ確定する。後出し予言の経路を作らない
8. 確証パートと探索パートを混ぜない。事前登録の判定ルール（§3.5）は登録後に変更禁止。探索由来のパターンを「実証」と表示しない
9. 願いの本文・成功基準・締切・層は登録後変更不可。割付は登録直後に自動実行し、人の選択を挟まない。取り下げは未実現として集計
10. 封印群の願いを実践UI（願いタイム含む）に表示する経路を作らない
11. 1PR = 1フェーズ = 1目的。PROGRESS.md 更新を伴わないPRは不可

## 9. キックオフプロンプト（P0用）

あなたはこのリポジトリの実装担当です。DESIGN.md（本設計図）を最初に読み、今回は【P0 足場】のみを実装してください。P1以降には着手しないこと。

**やること**:
1. Vite + React + TypeScript プロジェクト初期化（pnpm）
2. Capacitor 6 導入、iOSプラットフォーム追加（アプリ名: Intention Dice / bundle idは仮でOK）
3. @capacitor-community/sqlite を導入し、起動時に DB open → `ledger` テーブル作成まで配線（スキーマは DESIGN.md §5 の通り。まだ書き込み機能は作らない）
4. 画面はプレースホルダ1枚（「実験は準備中」＋ DB接続 OK/NG の表示）
5. `pnpm typecheck` / `pnpm test`（vitest, ダミーテスト1件）/ `pnpm simulate`（空実装で "not implemented" を出すだけ）のスクリプト整備
6. PROGRESS.md を作成し、P0の完了項目・未了項目・次フェーズへの申し送りを記録

**自己検証（必須）**:
- `pnpm typecheck && pnpm test` が green であることを実行結果付きで示す
- iOSシミュレータ or `pnpm dev` でプレースホルダ表示を確認した旨を報告
- DESIGN.md §8 の不変ルールに反するコードがないことを自己チェックして宣言

**禁止**:
- P1以降の機能の先行実装
- ledger への書き込み・削除コードの追加
- DESIGN.md の書き換え

完了したら PROGRESS.md を更新して終了報告してください。

## 10. 将来拡張（v2以降のメモ）

- 判定の盲検化: バージョン固定AIジャッジ（プロンプトのハッシュも台帳へ）または第三者判定 → Layer C を★★→★★★に近づける
- 他者の願いデザイン: 同意した友人が小さな願いを出し、ランダムに半分だけ実践、友人はどれが選ばれたか知らずに結果を報告（報告者ブラインド）。個人規模で作れる最も堅い形
- OpenTimestamps本格対応: チェーンヘッドをビットコインに刻む（無料）。自分にすら改竄不能に
- 敵対的協働: 開始前に懐疑的な人にプロトコルを見せ「この設計なら結果を認める」と署名してもらうフロー
- 匿名マルチユーザー集計（Cloudflare KV / D1）: 数百人×1年で Layer A ±0.05pp級・Layer C 微小効果級の検出力に到達
- 実験テンプレート共有: 他の人が同一プロトコルで追試でき、「再現性」まで扱える
